pipeline {
    agent any

    stages {
        stage('build and deploy') {
            steps {
                ansiblePlaybook(
                    playbook: 'create-configure-infa.yaml'
                    inventory: 'inventory.yaml' 
                    )
            }
        }
    }
}
