pipeline {
    agent any

    stages {
        stage('build and deploy') {
            steps {
                ansiblePlaybook inventory: 'inventory.yaml'
                ansiblePlaybook playbook: 'create-configure-infa.yaml'
            }
        }
    }
}
